import { space } from 'theme'
import { size } from '../board/style'

export const style = {
  root: {
    display: 'flex',
    width: size,
    'justify-content': 'space-between'
  },
  buttonRow: { margin: space[1], display: 'flex' }
}
